{
  "name": "db-prepare",
  "dependencies": {
    "types": "workspace:*",
    "prisma-client": "workspace:*",
    "utility": "workspace:*"
  },
  "scripts": {
    "populate": "bun run ./db-populate.ts"
  }
}
