{
  "name": "wordle-app",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --build --force",
    "dates": "workspace:*"
  },
  "devDependencies": {
    "encryption": "workspace:*",
    "types.app": "workspace:*",
    "types.utility": "workspace:*"
  }
}
